# Optimize math library in a loop.

run-script optimize1;
run-script optimize-loop;

if (optimize1.optimized != 0) {
   run-script optimize-loop;
}

while (optimize1.optimized != 0) {
    run-script optimize1;
}
