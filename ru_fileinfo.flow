import utctime;
import fs/filesystem;

export {
/*
	FcFileInfo(
		flowfile : string, // The path as used by flow imports
		trampstamp : double,
		fullpath : string, // The full filesystem path
		md5sum : string,
		mutable content : string,
		mutable gensymid : int,
		filesize : double
	);
*/
	RuFileInfo(
		file : string, // The path as used by flow imports
		time : double, // The timestamp of the source file
		//fullpath : string, // The full filesystem path
		//md5sum : string,
		//filesize : double,
		lastupdate : double, // The timestamp of the last successfull compilation
	);
	
	makeRuFileInfo(file : string) -> RuFileInfo {
		RuFileInfo(
			file,
			fileModified(file),
			timestamp()
		)
	}
}
