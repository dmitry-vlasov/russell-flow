import ds/tree;

export {
	RuPlace(file : string, pos : int);
	RuConf(
		file : string,
		findPaths : [string],
        storePaths : [string],
        verbose : int,
		opts : Tree<string, string>,
		threadId : int,
		onError : (string, [RuPlace]) -> void
	);
}
