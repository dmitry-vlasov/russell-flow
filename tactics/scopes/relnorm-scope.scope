// relnorm-scope — migrated from refiners.flow ruMizarRelnormScope(); this FILE takes precedence
// over the built-in fallback. One name per whitespace-separated token;
// '//' and '#' start comments. Curation rationale preserved below.

// difdifss ((A∖B)∖C ⊆ A∖C, miz_aux.ru): the dif32-canonical ∖∖-chain must
// match a schema against EITHER residual subtrahend; difss covers only one
ssid ssun1 ssun2 inss1 inss2 difss difdifss 0ss
