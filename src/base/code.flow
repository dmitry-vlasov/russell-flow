export {
	// Cached source code.
	RuCode(
		code : string,
		path : string,
		modified : double
	);
	ruNoCode = RuCode("", "", 0.0);

	ruCodeIsDefined(code : RuCode) -> bool;
}

ruCodeIsDefined(code : RuCode) -> bool {
	code.path != "";
}
