import ds/array;

export {
	native makeIntArray : (size: int) -> native = RussellIntArray.make;
	native sizeIntArray : (arr: native) -> int = RussellIntArray.size;
	native getIntArray : (arr: native, i: int) -> int = RussellIntArray.get;
	native setIntArray : (arr: native, i: int, v: int) -> int = RussellIntArray.set;
}
