import string;
import lingo/compiler/syntaxtree_types;

import ru/parse/mnemonics;

export {
	parse_source(t : string) -> Pair<SyntaxTree, ExpectError>;
}

parse_source(t : string) -> Pair<SyntaxTree, ExpectError> {
	acc = SyntaxParseAcc(t, strlen(t), 0, [], noExpectError);
	r = do_parse_source_source(acc);
	Pair(r, acc.expect);
}
do_parse_source_source(acc : SyntaxParseAcc) -> SyntaxTree {
	i = acc.i;
	if (getCharCodeAt(acc.input, acc.i) == 65279 /* ﻿ */) {
		acc.i ::= acc.i + 1;
	}
	c1 = do_parse_source_ws(acc);
	if (c1.choice != -1) {
		c2 = SyntaxStar(acc, do_parse_source_toplevel);
		SyntaxTree(st_source, 0, i, acc.i, [[c1], c2]);
	} else c1
}

do_parse_source_toplevel(acc : SyntaxParseAcc) -> SyntaxTree {
	i = acc.i;
	acc.i ::= i;
	r0 = {
		c0 = do_parse_source_scomment(acc);
		if (c0.choice != -1) {
			c0
		} else failSyntaxTree;
	}
	if (r0 != failSyntaxTree) r0
	else {
		acc.i ::= i;
		r1 = {
			c0 = do_parse_source_mcomment(acc);
			if (c0.choice != -1) {
				c0
			} else failSyntaxTree;
		}
		if (r1 != failSyntaxTree) r1
		else {
			acc.i ::= i;
			r2 = {
				c0 = do_parse_source_import(acc);
				if (c0.choice != -1) {
					c0
				} else failSyntaxTree;
			}
			if (r2 != failSyntaxTree) r2
			else {
				acc.i ::= i;
				r3 = {
					c0 = do_parse_source_const(acc);
					if (c0.choice != -1) {
						c0
					} else failSyntaxTree;
				}
				if (r3 != failSyntaxTree) r3
				else {
					acc.i ::= i;
					r4 = {
						c0 = do_parse_source_type(acc);
						if (c0.choice != -1) {
							c0
						} else failSyntaxTree;
					}
					if (r4 != failSyntaxTree) r4
					else {
						acc.i ::= i;
						r5 = {
							c0 = do_parse_source_rule(acc);
							if (c0.choice != -1) {
								c0
							} else failSyntaxTree;
						}
						if (r5 != failSyntaxTree) r5
						else {
							acc.i ::= i;
							r6 = {
								c0 = do_parse_source_axiom(acc);
								if (c0.choice != -1) {
									c0
								} else failSyntaxTree;
							}
							if (r6 != failSyntaxTree) r6
							else {
								acc.i ::= i;
								r7 = {
									c0 = do_parse_source_def(acc);
									if (c0.choice != -1) {
										c0
									} else failSyntaxTree;
								}
								if (r7 != failSyntaxTree) r7
								else {
									acc.i ::= i;
									r8 = {
										c0 = do_parse_source_theorem(acc);
										if (c0.choice != -1) {
											c0
										} else failSyntaxTree;
									}
									if (r8 != failSyntaxTree) r8
									else {
										SyntaxTree(st_Expected_toplevel, -1, i, i, [])
									}

								}

							}

						}

					}

				}

			}

		}

	}

}

do_parse_source_scomment(acc : SyntaxParseAcc) -> SyntaxTree {
	i = acc.i;
	if (getCharCodeAt(acc.input, acc.i) == 47 && getCharCodeAt(acc.input, acc.i + 1) == 47 /* // */) {
		acc.i ::= acc.i + 2;
		c1 = SyntaxStar(acc, do_parse_source_scomment__auto58);
		if (getCharCodeAt(acc.input, acc.i) == 10 /* 
		 */) {
			acc.i ::= acc.i + 1;
			c3 = do_parse_source_ws(acc);
			if (c3.choice != -1) {
				SyntaxTree(st_scomment, 0, i, acc.i, [c1, [c3]]);
			} else c3
		} else {
			// "Expected '\n'"
			updateExpectError(acc, "", "\n", i);
			failSyntaxTree;
		}
	} else {
		// "Expected '//'"
		updateExpectError(acc, "", "//", i);
		failSyntaxTree;
	}
}

do_parse_source_mcomment(acc : SyntaxParseAcc) -> SyntaxTree {
	i = acc.i;
	if (getCharCodeAt(acc.input, acc.i) == 47 && getCharCodeAt(acc.input, acc.i + 1) == 42 /* /* */) {
		acc.i ::= acc.i + 2;
		c1 = SyntaxStar(acc, do_parse_source_mcomment__auto59);
		if (getCharCodeAt(acc.input, acc.i) == 42 && getCharCodeAt(acc.input, acc.i + 1) == 47/* * / */) {
			acc.i ::= acc.i + 2;
			c3 = do_parse_source_ws(acc);
			if (c3.choice != -1) {
				SyntaxTree(st_mcomment, 0, i, acc.i, [c1, [c3]]);
			} else c3
		} else {
			// "Expected '*/'"
			updateExpectError(acc, "", "*/", i);
			failSyntaxTree;
		}
	} else {
		// "Expected '/*'"
		updateExpectError(acc, "", "/*", i);
		failSyntaxTree;
	}
}

do_parse_source_import(acc : SyntaxParseAcc) -> SyntaxTree {
	i = acc.i;
	if (strRangeIndexOf(acc.input, "import", acc.i, acc.i + 6) == acc.i) {
		acc.i ::= acc.i + 6;
		c1 = do_parse_source_ws(acc);
		if (c1.choice != -1) {
			c2 = do_parse_source_symb(acc);
			if (c2.choice != -1) {
				c3 = do_parse_source_ws(acc);
				if (c3.choice != -1) {
					if (getCharCodeAt(acc.input, acc.i) == 59 && getCharCodeAt(acc.input, acc.i + 1) == 59 /* ;; */) {
						acc.i ::= acc.i + 2;
						c5 = do_parse_source_ws(acc);
						if (c5.choice != -1) {
							SyntaxTree(st_import, 0, i, acc.i, [[c1], [c2], [c3], [c5]]);
						} else c5
					} else {
						// "Expected ';;'"
						updateExpectError(acc, "", ";;", i);
						failSyntaxTree;
					}
				} else c3
			} else c2
		} else c1
	} else {
		// "Expected 'import'"
		updateExpectError(acc, "", "import", i);
		failSyntaxTree;
	}
}

do_parse_source_const(acc : SyntaxParseAcc) -> SyntaxTree {
	i = acc.i;
	if (strRangeIndexOf(acc.input, "constant", acc.i, acc.i + 8) == acc.i) {
		acc.i ::= acc.i + 8;
		c1 = do_parse_source_ws(acc);
		if (c1.choice != -1) {
			if (getCharCodeAt(acc.input, acc.i) == 123 /* { */) {
				acc.i ::= acc.i + 1;
				c3 = do_parse_source_ws(acc);
				if (c3.choice != -1) {
					c4 = do_parse_source_symbol(acc);
					if (c4.choice != -1) {
						i5 = acc.i;
						t5 = do_parse_source_ascii(acc);
						c5 = if (t5.choice == -1) {acc.i ::= i5; [] } else [t5];
						i6 = acc.i;
						t6 = do_parse_source_latex(acc);
						c6 = if (t6.choice == -1) {acc.i ::= i6; [] } else [t6];
						if (getCharCodeAt(acc.input, acc.i) == 125 /* } */) {
							acc.i ::= acc.i + 1;
							c8 = do_parse_source_ws(acc);
							if (c8.choice != -1) {
								SyntaxTree(st_const, 0, i, acc.i, [[c1], [c3], [c4], c5, c6, [c8]]);
							} else c8
						} else {
							// "Expected '}'"
							updateExpectError(acc, "", "}", i);
							failSyntaxTree;
						}
					} else c4
				} else c3
			} else {
				// "Expected '{'"
				updateExpectError(acc, "", "{", i);
				failSyntaxTree;
			}
		} else c1
	} else {
		// "Expected 'constant'"
		updateExpectError(acc, "", "constant", i);
		failSyntaxTree;
	}
}

do_parse_source_symbol(acc : SyntaxParseAcc) -> SyntaxTree {
	i = acc.i;
	if (strRangeIndexOf(acc.input, "symbol", acc.i, acc.i + 6) == acc.i) {
		acc.i ::= acc.i + 6;
		c1 = do_parse_source_ws(acc);
		if (c1.choice != -1) {
			c2 = do_parse_source_symb(acc);
			if (c2.choice != -1) {
				c3 = do_parse_source_ws(acc);
				if (c3.choice != -1) {
					if (getCharCodeAt(acc.input, acc.i) == 59 && getCharCodeAt(acc.input, acc.i + 1) == 59 /* ;; */) {
						acc.i ::= acc.i + 2;
						c5 = do_parse_source_ws(acc);
						if (c5.choice != -1) {
							SyntaxTree(st_symbol, 0, i, acc.i, [[c1], [c2], [c3], [c5]]);
						} else c5
					} else {
						// "Expected ';;'"
						updateExpectError(acc, "", ";;", i);
						failSyntaxTree;
					}
				} else c3
			} else c2
		} else c1
	} else {
		// "Expected 'symbol'"
		updateExpectError(acc, "", "symbol", i);
		failSyntaxTree;
	}
}

do_parse_source_ascii(acc : SyntaxParseAcc) -> SyntaxTree {
	i = acc.i;
	if (strRangeIndexOf(acc.input, "ascii", acc.i, acc.i + 5) == acc.i) {
		acc.i ::= acc.i + 5;
		c1 = do_parse_source_ws(acc);
		if (c1.choice != -1) {
			c2 = do_parse_source_symb(acc);
			if (c2.choice != -1) {
				c3 = do_parse_source_ws(acc);
				if (c3.choice != -1) {
					if (getCharCodeAt(acc.input, acc.i) == 59 && getCharCodeAt(acc.input, acc.i + 1) == 59 /* ;; */) {
						acc.i ::= acc.i + 2;
						c5 = do_parse_source_ws(acc);
						if (c5.choice != -1) {
							SyntaxTree(st_ascii, 0, i, acc.i, [[c1], [c2], [c3], [c5]]);
						} else c5
					} else {
						// "Expected ';;'"
						updateExpectError(acc, "", ";;", i);
						failSyntaxTree;
					}
				} else c3
			} else c2
		} else c1
	} else {
		// "Expected 'ascii'"
		updateExpectError(acc, "", "ascii", i);
		failSyntaxTree;
	}
}

do_parse_source_latex(acc : SyntaxParseAcc) -> SyntaxTree {
	i = acc.i;
	if (strRangeIndexOf(acc.input, "latex", acc.i, acc.i + 5) == acc.i) {
		acc.i ::= acc.i + 5;
		c1 = do_parse_source_ws(acc);
		if (c1.choice != -1) {
			c2 = do_parse_source_symb(acc);
			if (c2.choice != -1) {
				c3 = do_parse_source_ws(acc);
				if (c3.choice != -1) {
					if (getCharCodeAt(acc.input, acc.i) == 59 && getCharCodeAt(acc.input, acc.i + 1) == 59 /* ;; */) {
						acc.i ::= acc.i + 2;
						c5 = do_parse_source_ws(acc);
						if (c5.choice != -1) {
							SyntaxTree(st_latex, 0, i, acc.i, [[c1], [c2], [c3], [c5]]);
						} else c5
					} else {
						// "Expected ';;'"
						updateExpectError(acc, "", ";;", i);
						failSyntaxTree;
					}
				} else c3
			} else c2
		} else c1
	} else {
		// "Expected 'latex'"
		updateExpectError(acc, "", "latex", i);
		failSyntaxTree;
	}
}

do_parse_source_type(acc : SyntaxParseAcc) -> SyntaxTree {
	i = acc.i;
	if (strRangeIndexOf(acc.input, "type", acc.i, acc.i + 4) == acc.i) {
		acc.i ::= acc.i + 4;
		c1 = do_parse_source_ws(acc);
		if (c1.choice != -1) {
			c2 = do_parse_source_id(acc);
			if (c2.choice != -1) {
				c3 = do_parse_source_ws(acc);
				if (c3.choice != -1) {
					i4 = acc.i;
					t4 = do_parse_source_supers(acc);
					c4 = if (t4.choice == -1) {acc.i ::= i4; [] } else [t4];
					if (getCharCodeAt(acc.input, acc.i) == 59 && getCharCodeAt(acc.input, acc.i + 1) == 59 /* ;; */) {
						acc.i ::= acc.i + 2;
						c6 = do_parse_source_ws(acc);
						if (c6.choice != -1) {
							SyntaxTree(st_type, 0, i, acc.i, [[c1], [c2], [c3], c4, [c6]]);
						} else c6
					} else {
						// "Expected ';;'"
						updateExpectError(acc, "", ";;", i);
						failSyntaxTree;
					}
				} else c3
			} else c2
		} else c1
	} else {
		// "Expected 'type'"
		updateExpectError(acc, "", "type", i);
		failSyntaxTree;
	}
}

do_parse_source_supers(acc : SyntaxParseAcc) -> SyntaxTree {
	i = acc.i;
	if (getCharCodeAt(acc.input, acc.i) == 58 /* : */) {
		acc.i ::= acc.i + 1;
		c1 = do_parse_source_ws(acc);
		if (c1.choice != -1) {
			c2 = do_parse_source_id(acc);
			if (c2.choice != -1) {
				c3 = do_parse_source_ws(acc);
				if (c3.choice != -1) {
					c4 = SyntaxStar(acc, do_parse_source_super);
					SyntaxTree(st_supers, 0, i, acc.i, [[c1], [c2], [c3], c4]);
				} else c3
			} else c2
		} else c1
	} else {
		// "Expected ':'"
		updateExpectError(acc, "", ":", i);
		failSyntaxTree;
	}
}

do_parse_source_super(acc : SyntaxParseAcc) -> SyntaxTree {
	i = acc.i;
	if (getCharCodeAt(acc.input, acc.i) == 44 /* , */) {
		acc.i ::= acc.i + 1;
		c1 = do_parse_source_ws(acc);
		if (c1.choice != -1) {
			c2 = do_parse_source_id(acc);
			if (c2.choice != -1) {
				c3 = do_parse_source_ws(acc);
				if (c3.choice != -1) {
					SyntaxTree(st_super, 0, i, acc.i, [[c1], [c2], [c3]]);
				} else c3
			} else c2
		} else c1
	} else {
		// "Expected ','"
		updateExpectError(acc, "", ",", i);
		failSyntaxTree;
	}
}

do_parse_source_rule(acc : SyntaxParseAcc) -> SyntaxTree {
	i = acc.i;
	if (strRangeIndexOf(acc.input, "rule", acc.i, acc.i + 4) == acc.i) {
		acc.i ::= acc.i + 4;
		c1 = do_parse_source_ws(acc);
		if (c1.choice != -1) {
			c2 = do_parse_source_id(acc);
			if (c2.choice != -1) {
				c3 = do_parse_source_ws(acc);
				if (c3.choice != -1) {
					i4 = acc.i;
					t4 = do_parse_source_vars(acc);
					c4 = if (t4.choice == -1) {acc.i ::= i4; [] } else [t4];
					if (getCharCodeAt(acc.input, acc.i) == 123 /* { */) {
						acc.i ::= acc.i + 1;
						c6 = do_parse_source_ws(acc);
						if (c6.choice != -1) {
							if (strRangeIndexOf(acc.input, "term", acc.i, acc.i + 4) == acc.i) {
								acc.i ::= acc.i + 4;
								c8 = do_parse_source_ws(acc);
								if (c8.choice != -1) {
									c9 = do_parse_source_expr(acc);
									if (c9.choice != -1) {
										if (getCharCodeAt(acc.input, acc.i) == 125 /* } */) {
											acc.i ::= acc.i + 1;
											c11 = do_parse_source_ws(acc);
											if (c11.choice != -1) {
												SyntaxTree(st_rule, 0, i, acc.i, [[c1], [c2], [c3], c4, [c6], [c8], [c9], [c11]]);
											} else c11
										} else {
											// "Expected '}'"
											updateExpectError(acc, "", "}", i);
											failSyntaxTree;
										}
									} else c9
								} else c8
							} else {
								// "Expected 'term'"
								updateExpectError(acc, "", "term", i);
								failSyntaxTree;
							}
						} else c6
					} else {
						// "Expected '{'"
						updateExpectError(acc, "", "{", i);
						failSyntaxTree;
					}
				} else c3
			} else c2
		} else c1
	} else {
		// "Expected 'rule'"
		updateExpectError(acc, "", "rule", i);
		failSyntaxTree;
	}
}

do_parse_source_axiom(acc : SyntaxParseAcc) -> SyntaxTree {
	i = acc.i;
	if (strRangeIndexOf(acc.input, "axiom", acc.i, acc.i + 5) == acc.i) {
		acc.i ::= acc.i + 5;
		c1 = do_parse_source_ws(acc);
		if (c1.choice != -1) {
			c2 = do_parse_source_id(acc);
			if (c2.choice != -1) {
				c3 = do_parse_source_ws(acc);
				if (c3.choice != -1) {
					i4 = acc.i;
					t4 = do_parse_source_vars(acc);
					c4 = if (t4.choice == -1) {acc.i ::= i4; [] } else [t4];
					i5 = acc.i;
					t5 = do_parse_source_disjs(acc);
					c5 = if (t5.choice == -1) {acc.i ::= i5; [] } else [t5];
					if (getCharCodeAt(acc.input, acc.i) == 123 /* { */) {
						acc.i ::= acc.i + 1;
						c7 = do_parse_source_ws(acc);
						if (c7.choice != -1) {
							i8 = acc.i;
							t8 = do_parse_source_hyps_bar(acc);
							c8 = if (t8.choice == -1) {acc.i ::= i8; [] } else [t8];
							if (strRangeIndexOf(acc.input, "prop", acc.i, acc.i + 4) == acc.i) {
								acc.i ::= acc.i + 4;
								c10 = do_parse_source_ws(acc);
								if (c10.choice != -1) {
									c11 = do_parse_source_expr(acc);
									if (c11.choice != -1) {
										if (getCharCodeAt(acc.input, acc.i) == 125 /* } */) {
											acc.i ::= acc.i + 1;
											c13 = do_parse_source_ws(acc);
											if (c13.choice != -1) {
												SyntaxTree(st_axiom, 0, i, acc.i, [[c1], [c2], [c3], c4, c5, [c7], c8, [c10], [c11], [c13]]);
											} else c13
										} else {
											// "Expected '}'"
											updateExpectError(acc, "", "}", i);
											failSyntaxTree;
										}
									} else c11
								} else c10
							} else {
								// "Expected 'prop'"
								updateExpectError(acc, "", "prop", i);
								failSyntaxTree;
							}
						} else c7
					} else {
						// "Expected '{'"
						updateExpectError(acc, "", "{", i);
						failSyntaxTree;
					}
				} else c3
			} else c2
		} else c1
	} else {
		// "Expected 'axiom'"
		updateExpectError(acc, "", "axiom", i);
		failSyntaxTree;
	}
}

do_parse_source_def(acc : SyntaxParseAcc) -> SyntaxTree {
	i = acc.i;
	if (strRangeIndexOf(acc.input, "definition", acc.i, acc.i + 10) == acc.i) {
		acc.i ::= acc.i + 10;
		c1 = do_parse_source_ws(acc);
		if (c1.choice != -1) {
			c2 = do_parse_source_id(acc);
			if (c2.choice != -1) {
				c3 = do_parse_source_ws(acc);
				if (c3.choice != -1) {
					i4 = acc.i;
					t4 = do_parse_source_vars(acc);
					c4 = if (t4.choice == -1) {acc.i ::= i4; [] } else [t4];
					i5 = acc.i;
					t5 = do_parse_source_disjs(acc);
					c5 = if (t5.choice == -1) {acc.i ::= i5; [] } else [t5];
					i6 = acc.i;
					t6 = do_parse_source_meta(acc);
					c6 = if (t6.choice == -1) {acc.i ::= i6; [] } else [t6];
					if (getCharCodeAt(acc.input, acc.i) == 123 /* { */) {
						acc.i ::= acc.i + 1;
						c8 = do_parse_source_ws(acc);
						if (c8.choice != -1) {
							c9 = SyntaxStar(acc, do_parse_source_hyp);
							c10 = do_parse_source_defm(acc);
							if (c10.choice != -1) {
								c11 = do_parse_source_defs(acc);
								if (c11.choice != -1) {
									if (strRangeIndexOf(acc.input, "-----", acc.i, acc.i + 5) == acc.i) {
										acc.i ::= acc.i + 5;
										SyntaxStar(acc, \na ->
											if (getCharCodeAt(acc.input, acc.i) == 45 /* - */) {
												acc.i ::= acc.i + 1;
												dummySyntaxTree;
											} else {
												failSyntaxTree
											}
										);
										c14 = do_parse_source_ws(acc);
										if (c14.choice != -1) {
											if (strRangeIndexOf(acc.input, "prop", acc.i, acc.i + 4) == acc.i) {
												acc.i ::= acc.i + 4;
												c16 = do_parse_source_ws(acc);
												if (c16.choice != -1) {
													c17 = do_parse_source_expr(acc);
													if (c17.choice != -1) {
														if (getCharCodeAt(acc.input, acc.i) == 125 /* } */) {
															acc.i ::= acc.i + 1;
															c19 = do_parse_source_ws(acc);
															if (c19.choice != -1) {
																SyntaxTree(st_def, 0, i, acc.i, [[c1], [c2], [c3], c4, c5, c6, [c8], c9, [c10], [c11], [c14], [c16], [c17], [c19]]);
															} else c19
														} else {
															// "Expected '}'"
															updateExpectError(acc, "", "}", i);
															failSyntaxTree;
														}
													} else c17
												} else c16
											} else {
												// "Expected 'prop'"
												updateExpectError(acc, "", "prop", i);
												failSyntaxTree;
											}
										} else c14
									} else {
										// "Expected '-----'"
										updateExpectError(acc, "", "-----", i);
										failSyntaxTree;
									}
								} else c11
							} else c10
						} else c8
					} else {
						// "Expected '{'"
						updateExpectError(acc, "", "{", i);
						failSyntaxTree;
					}
				} else c3
			} else c2
		} else c1
	} else {
		// "Expected 'definition'"
		updateExpectError(acc, "", "definition", i);
		failSyntaxTree;
	}
}

do_parse_source_theorem(acc : SyntaxParseAcc) -> SyntaxTree {
	i = acc.i;
	if (strRangeIndexOf(acc.input, "theorem", acc.i, acc.i + 7) == acc.i) {
		acc.i ::= acc.i + 7;
		c1 = do_parse_source_ws(acc);
		if (c1.choice != -1) {
			c2 = do_parse_source_id(acc);
			if (c2.choice != -1) {
				c3 = do_parse_source_ws(acc);
				if (c3.choice != -1) {
					i4 = acc.i;
					t4 = do_parse_source_vars(acc);
					c4 = if (t4.choice == -1) {acc.i ::= i4; [] } else [t4];
					i5 = acc.i;
					t5 = do_parse_source_disjs(acc);
					c5 = if (t5.choice == -1) {acc.i ::= i5; [] } else [t5];
					if (getCharCodeAt(acc.input, acc.i) == 123 /* { */) {
						acc.i ::= acc.i + 1;
						c7 = do_parse_source_ws(acc);
						if (c7.choice != -1) {
							i8 = acc.i;
							t8 = do_parse_source_hyps_bar(acc);
							c8 = if (t8.choice == -1) {acc.i ::= i8; [] } else [t8];
							if (strRangeIndexOf(acc.input, "prop", acc.i, acc.i + 4) == acc.i) {
								acc.i ::= acc.i + 4;
								c10 = do_parse_source_ws(acc);
								if (c10.choice != -1) {
									c11 = do_parse_source_expr(acc);
									if (c11.choice != -1) {
										if (getCharCodeAt(acc.input, acc.i) == 125 /* } */) {
											acc.i ::= acc.i + 1;
											c13 = do_parse_source_ws(acc);
											if (c13.choice != -1) {
												c14 = do_parse_source_proof(acc);
												if (c14.choice != -1) {
													SyntaxTree(st_theorem, 0, i, acc.i, [[c1], [c2], [c3], c4, c5, [c7], c8, [c10], [c11], [c13], [c14]]);
												} else c14
											} else c13
										} else {
											// "Expected '}'"
											updateExpectError(acc, "", "}", i);
											failSyntaxTree;
										}
									} else c11
								} else c10
							} else {
								// "Expected 'prop'"
								updateExpectError(acc, "", "prop", i);
								failSyntaxTree;
							}
						} else c7
					} else {
						// "Expected '{'"
						updateExpectError(acc, "", "{", i);
						failSyntaxTree;
					}
				} else c3
			} else c2
		} else c1
	} else {
		// "Expected 'theorem'"
		updateExpectError(acc, "", "theorem", i);
		failSyntaxTree;
	}
}

do_parse_source_proof(acc : SyntaxParseAcc) -> SyntaxTree {
	i = acc.i;
	if (strRangeIndexOf(acc.input, "proof", acc.i, acc.i + 5) == acc.i) {
		acc.i ::= acc.i + 5;
		c1 = do_parse_source_ws(acc);
		if (c1.choice != -1) {
			i2 = acc.i;
			t2 = do_parse_source_vars(acc);
			c2 = if (t2.choice == -1) {acc.i ::= i2; [] } else [t2];
			i3 = acc.i;
			t3 = do_parse_source_disjs(acc);
			c3 = if (t3.choice == -1) {acc.i ::= i3; [] } else [t3];
			if (getCharCodeAt(acc.input, acc.i) == 123 /* { */) {
				acc.i ::= acc.i + 1;
				c5 = do_parse_source_ws(acc);
				if (c5.choice != -1) {
					c6 = SyntaxStar(acc, do_parse_source_step);
					if (getCharCodeAt(acc.input, acc.i) == 125 /* } */) {
						acc.i ::= acc.i + 1;
						c8 = do_parse_source_ws(acc);
						if (c8.choice != -1) {
							SyntaxTree(st_proof, 0, i, acc.i, [[c1], c2, c3, [c5], c6, [c8]]);
						} else c8
					} else {
						// "Expected '}'"
						updateExpectError(acc, "", "}", i);
						failSyntaxTree;
					}
				} else c5
			} else {
				// "Expected '{'"
				updateExpectError(acc, "", "{", i);
				failSyntaxTree;
			}
		} else c1
	} else {
		// "Expected 'proof'"
		updateExpectError(acc, "", "proof", i);
		failSyntaxTree;
	}
}

do_parse_source_hyps_bar(acc : SyntaxParseAcc) -> SyntaxTree {
	i = acc.i;
	c0 = SyntaxPlus(acc, do_parse_source_hyp);
	if (length(c0) >= 1) {
		if (strRangeIndexOf(acc.input, "-----", acc.i, acc.i + 5) == acc.i) {
			acc.i ::= acc.i + 5;
			SyntaxStar(acc, \na ->
				if (getCharCodeAt(acc.input, acc.i) == 45 /* - */) {
					acc.i ::= acc.i + 1;
					dummySyntaxTree;
				} else {
					failSyntaxTree
				}
			);
			c3 = do_parse_source_ws(acc);
			if (c3.choice != -1) {
				SyntaxTree(st_hyps_bar, 0, i, acc.i, [c0, [c3]]);
			} else c3
		} else {
			// "Expected '-----'"
			updateExpectError(acc, "", "-----", i);
			failSyntaxTree;
		}
	} else {
		// "Expected at least one hyp"
		updateExpectError(acc, "", "hyp", i);
		failSyntaxTree;
	}
}

do_parse_source_hyps(acc : SyntaxParseAcc) -> SyntaxTree {
	i = acc.i;
	c0 = SyntaxPlus(acc, do_parse_source_hyp);
	if (length(c0) >= 1) {
		SyntaxTree(st_hyps, 0, i, acc.i, [c0]);
	} else {
		// "Expected at least one hyp"
		updateExpectError(acc, "", "hyp", i);
		failSyntaxTree;
	}
}

do_parse_source_hyp(acc : SyntaxParseAcc) -> SyntaxTree {
	i = acc.i;
	if (strRangeIndexOf(acc.input, "hyp", acc.i, acc.i + 3) == acc.i) {
		acc.i ::= acc.i + 3;
		c1 = do_parse_source_ws(acc);
		if (c1.choice != -1) {
			c2 = do_parse_source_ind(acc);
			if (c2.choice != -1) {
				c3 = do_parse_source_ws(acc);
				if (c3.choice != -1) {
					c4 = do_parse_source_expr(acc);
					if (c4.choice != -1) {
						SyntaxTree(st_hyp, 0, i, acc.i, [[c1], [c2], [c3], [c4]]);
					} else c4
				} else c3
			} else c2
		} else c1
	} else {
		// "Expected 'hyp'"
		updateExpectError(acc, "", "hyp", i);
		failSyntaxTree;
	}
}

do_parse_source_defm(acc : SyntaxParseAcc) -> SyntaxTree {
	i = acc.i;
	if (strRangeIndexOf(acc.input, "defiendum", acc.i, acc.i + 9) == acc.i) {
		acc.i ::= acc.i + 9;
		c1 = do_parse_source_ws(acc);
		if (c1.choice != -1) {
			c2 = do_parse_source_expr(acc);
			if (c2.choice != -1) {
				SyntaxTree(st_defm, 0, i, acc.i, [[c1], [c2]]);
			} else c2
		} else c1
	} else {
		// "Expected 'defiendum'"
		updateExpectError(acc, "", "defiendum", i);
		failSyntaxTree;
	}
}

do_parse_source_defs(acc : SyntaxParseAcc) -> SyntaxTree {
	i = acc.i;
	if (strRangeIndexOf(acc.input, "definiens", acc.i, acc.i + 9) == acc.i) {
		acc.i ::= acc.i + 9;
		c1 = do_parse_source_ws(acc);
		if (c1.choice != -1) {
			c2 = do_parse_source_expr(acc);
			if (c2.choice != -1) {
				SyntaxTree(st_defs, 0, i, acc.i, [[c1], [c2]]);
			} else c2
		} else c1
	} else {
		// "Expected 'definiens'"
		updateExpectError(acc, "", "definiens", i);
		failSyntaxTree;
	}
}

do_parse_source_step(acc : SyntaxParseAcc) -> SyntaxTree {
	i = acc.i;
	if (strRangeIndexOf(acc.input, "step", acc.i, acc.i + 4) == acc.i) {
		acc.i ::= acc.i + 4;
		c1 = do_parse_source_ws(acc);
		if (c1.choice != -1) {
			c2 = do_parse_source_ind(acc);
			if (c2.choice != -1) {
				c3 = do_parse_source_ws(acc);
				if (c3.choice != -1) {
					c4 = do_parse_source_type_id(acc);
					if (c4.choice != -1) {
						if (getCharCodeAt(acc.input, acc.i) == 61 /* = */) {
							acc.i ::= acc.i + 1;
							c6 = do_parse_source_ws(acc);
							if (c6.choice != -1) {
								c7 = do_parse_source_id(acc);
								if (c7.choice != -1) {
									c8 = do_parse_source_ws(acc);
									if (c8.choice != -1) {
										c9 = do_parse_source_refs(acc);
										if (c9.choice != -1) {
											if (getCharCodeAt(acc.input, acc.i) == 124 && getCharCodeAt(acc.input, acc.i + 1) == 45 /* |- */) {
												acc.i ::= acc.i + 2;
												c11 = do_parse_source_ws(acc);
												if (c11.choice != -1) {
													c12 = SyntaxPlus(acc, do_parse_source_symbws);
													if (length(c12) >= 1) {
														if (getCharCodeAt(acc.input, acc.i) == 59 && getCharCodeAt(acc.input, acc.i + 1) == 59 /* ;; */) {
															acc.i ::= acc.i + 2;
															c14 = do_parse_source_ws(acc);
															if (c14.choice != -1) {
																SyntaxTree(st_step, 0, i, acc.i, [[c1], [c2], [c3], [c4], [c6], [c7], [c8], [c9], [c11], c12, [c14]]);
															} else c14
														} else {
															// "Expected ';;'"
															updateExpectError(acc, "", ";;", i);
															failSyntaxTree;
														}
													} else {
														// "Expected at least one symbws"
														updateExpectError(acc, "", "symbws", i);
														failSyntaxTree;
													}
												} else c11
											} else {
												// "Expected '|-'"
												updateExpectError(acc, "", "|-", i);
												failSyntaxTree;
											}
										} else c9
									} else c8
								} else c7
							} else c6
						} else {
							// "Expected '='"
							updateExpectError(acc, "", "=", i);
							failSyntaxTree;
						}
					} else c4
				} else c3
			} else c2
		} else c1
	} else {
		// "Expected 'step'"
		updateExpectError(acc, "", "step", i);
		failSyntaxTree;
	}
}

do_parse_source_refs(acc : SyntaxParseAcc) -> SyntaxTree {
	i = acc.i;
	if (getCharCodeAt(acc.input, acc.i) == 40 /* ( */) {
		acc.i ::= acc.i + 1;
		c1 = do_parse_source_ws(acc);
		if (c1.choice != -1) {
			i2 = acc.i;
			t2 = do_parse_source_refs0(acc);
			c2 = if (t2.choice == -1) {acc.i ::= i2; [] } else [t2];
			if (getCharCodeAt(acc.input, acc.i) == 41 /* ) */) {
				acc.i ::= acc.i + 1;
				c4 = do_parse_source_ws(acc);
				if (c4.choice != -1) {
					SyntaxTree(st_refs, 0, i, acc.i, [[c1], c2, [c4]]);
				} else c4
			} else {
				// "Expected ')'"
				updateExpectError(acc, "", ")", i);
				failSyntaxTree;
			}
		} else c1
	} else {
		// "Expected '('"
		updateExpectError(acc, "", "(", i);
		failSyntaxTree;
	}
}

do_parse_source_refs0(acc : SyntaxParseAcc) -> SyntaxTree {
	i = acc.i;
	c0 = do_parse_source_ref(acc);
	if (c0.choice != -1) {
		c1 = SyntaxStar(acc, do_parse_source_refs1);
		SyntaxTree(st_refs0, 0, i, acc.i, [[c0], c1]);
	} else c0
}

do_parse_source_refs1(acc : SyntaxParseAcc) -> SyntaxTree {
	i = acc.i;
	if (getCharCodeAt(acc.input, acc.i) == 44 /* , */) {
		acc.i ::= acc.i + 1;
		c1 = do_parse_source_ws(acc);
		if (c1.choice != -1) {
			c2 = do_parse_source_ref(acc);
			if (c2.choice != -1) {
				SyntaxTree(st_refs1, 0, i, acc.i, [[c1], [c2]]);
			} else c2
		} else c1
	} else {
		// "Expected ','"
		updateExpectError(acc, "", ",", i);
		failSyntaxTree;
	}
}

do_parse_source_ref(acc : SyntaxParseAcc) -> SyntaxTree {
	i = acc.i;
	acc.i ::= i;
	r0 = {
		c0 = do_parse_source_hyp_ref(acc);
		if (c0.choice != -1) {
			c0
		} else failSyntaxTree;
	}
	if (r0 != failSyntaxTree) r0
	else {
		acc.i ::= i;
		r1 = {
			c0 = do_parse_source_step_ref(acc);
			if (c0.choice != -1) {
				c0
			} else failSyntaxTree;
		}
		if (r1 != failSyntaxTree) r1
		else {
			SyntaxTree(st_Expected_ref, -1, i, i, [])
		}

	}

}

do_parse_source_hyp_ref(acc : SyntaxParseAcc) -> SyntaxTree {
	i = acc.i;
	if (strRangeIndexOf(acc.input, "hyp", acc.i, acc.i + 3) == acc.i) {
		acc.i ::= acc.i + 3;
		c1 = do_parse_source_ws(acc);
		if (c1.choice != -1) {
			c2 = do_parse_source_ind(acc);
			if (c2.choice != -1) {
				c3 = do_parse_source_ws(acc);
				if (c3.choice != -1) {
					SyntaxTree(st_hyp_ref, 0, i, acc.i, [[c1], [c2], [c3]]);
				} else c3
			} else c2
		} else c1
	} else {
		// "Expected 'hyp'"
		updateExpectError(acc, "", "hyp", i);
		failSyntaxTree;
	}
}

do_parse_source_step_ref(acc : SyntaxParseAcc) -> SyntaxTree {
	i = acc.i;
	if (strRangeIndexOf(acc.input, "step", acc.i, acc.i + 4) == acc.i) {
		acc.i ::= acc.i + 4;
		c1 = do_parse_source_ws(acc);
		if (c1.choice != -1) {
			c2 = do_parse_source_ind(acc);
			if (c2.choice != -1) {
				c3 = do_parse_source_ws(acc);
				if (c3.choice != -1) {
					SyntaxTree(st_step_ref, 0, i, acc.i, [[c1], [c2], [c3]]);
				} else c3
			} else c2
		} else c1
	} else {
		// "Expected 'step'"
		updateExpectError(acc, "", "step", i);
		failSyntaxTree;
	}
}

do_parse_source_disjs(acc : SyntaxParseAcc) -> SyntaxTree {
	i = acc.i;
	if (strRangeIndexOf(acc.input, "disjointed", acc.i, acc.i + 10) == acc.i) {
		acc.i ::= acc.i + 10;
		c1 = do_parse_source_ws(acc);
		if (c1.choice != -1) {
			if (getCharCodeAt(acc.input, acc.i) == 40 /* ( */) {
				acc.i ::= acc.i + 1;
				c3 = do_parse_source_ws(acc);
				if (c3.choice != -1) {
					i4 = acc.i;
					t4 = do_parse_source_disjs0(acc);
					c4 = if (t4.choice == -1) {acc.i ::= i4; [] } else [t4];
					if (getCharCodeAt(acc.input, acc.i) == 41 /* ) */) {
						acc.i ::= acc.i + 1;
						c6 = do_parse_source_ws(acc);
						if (c6.choice != -1) {
							SyntaxTree(st_disjs, 0, i, acc.i, [[c1], [c3], c4, [c6]]);
						} else c6
					} else {
						// "Expected ')'"
						updateExpectError(acc, "", ")", i);
						failSyntaxTree;
					}
				} else c3
			} else {
				// "Expected '('"
				updateExpectError(acc, "", "(", i);
				failSyntaxTree;
			}
		} else c1
	} else {
		// "Expected 'disjointed'"
		updateExpectError(acc, "", "disjointed", i);
		failSyntaxTree;
	}
}

do_parse_source_disjs0(acc : SyntaxParseAcc) -> SyntaxTree {
	i = acc.i;
	c0 = do_parse_source_disj(acc);
	if (c0.choice != -1) {
		c1 = SyntaxStar(acc, do_parse_source_disjs1);
		SyntaxTree(st_disjs0, 0, i, acc.i, [[c0], c1]);
	} else c0
}

do_parse_source_disjs1(acc : SyntaxParseAcc) -> SyntaxTree {
	i = acc.i;
	if (getCharCodeAt(acc.input, acc.i) == 44 /* , */) {
		acc.i ::= acc.i + 1;
		c1 = do_parse_source_ws(acc);
		if (c1.choice != -1) {
			c2 = do_parse_source_disj(acc);
			if (c2.choice != -1) {
				SyntaxTree(st_disjs1, 0, i, acc.i, [[c1], [c2]]);
			} else c2
		} else c1
	} else {
		// "Expected ','"
		updateExpectError(acc, "", ",", i);
		failSyntaxTree;
	}
}

do_parse_source_disj(acc : SyntaxParseAcc) -> SyntaxTree {
	i = acc.i;
	c0 = do_parse_source_dis0_v(acc);
	if (c0.choice != -1) {
		c1 = SyntaxPlus(acc, do_parse_source_dis1_v);
		if (length(c1) >= 1) {
			SyntaxTree(st_disj, 0, i, acc.i, [[c0], c1]);
		} else {
			// "Expected at least one dis1_v"
			updateExpectError(acc, "", "dis1_v", i);
			failSyntaxTree;
		}
	} else c0
}

do_parse_source_dis0_v(acc : SyntaxParseAcc) -> SyntaxTree {
	i = acc.i;
	c0 = do_parse_source_var_id(acc);
	if (c0.choice != -1) {
		c1 = do_parse_source_ws(acc);
		if (c1.choice != -1) {
			SyntaxTree(st_dis0_v, 0, i, acc.i, [[c0], [c1]]);
		} else c1
	} else c0
}

do_parse_source_dis1_v(acc : SyntaxParseAcc) -> SyntaxTree {
	i = acc.i;
	c0 = do_parse_source_var_id(acc);
	if (c0.choice != -1) {
		c1 = do_parse_source_ws(acc);
		if (c1.choice != -1) {
			SyntaxTree(st_dis1_v, 0, i, acc.i, [[c0], [c1]]);
		} else c1
	} else c0
}

do_parse_source_meta(acc : SyntaxParseAcc) -> SyntaxTree {
	i = acc.i;
	if (strRangeIndexOf(acc.input, "meta", acc.i, acc.i + 4) == acc.i) {
		acc.i ::= acc.i + 4;
		c1 = do_parse_source_ws(acc);
		if (c1.choice != -1) {
			if (getCharCodeAt(acc.input, acc.i) == 40 /* ( */) {
				acc.i ::= acc.i + 1;
				c3 = do_parse_source_ws(acc);
				if (c3.choice != -1) {
					i4 = acc.i;
					t4 = do_parse_source_meta0(acc);
					c4 = if (t4.choice == -1) {acc.i ::= i4; [] } else [t4];
					if (getCharCodeAt(acc.input, acc.i) == 41 /* ) */) {
						acc.i ::= acc.i + 1;
						c6 = do_parse_source_ws(acc);
						if (c6.choice != -1) {
							SyntaxTree(st_meta, 0, i, acc.i, [[c1], [c3], c4, [c6]]);
						} else c6
					} else {
						// "Expected ')'"
						updateExpectError(acc, "", ")", i);
						failSyntaxTree;
					}
				} else c3
			} else {
				// "Expected '('"
				updateExpectError(acc, "", "(", i);
				failSyntaxTree;
			}
		} else c1
	} else {
		// "Expected 'meta'"
		updateExpectError(acc, "", "meta", i);
		failSyntaxTree;
	}
}

do_parse_source_meta0(acc : SyntaxParseAcc) -> SyntaxTree {
	i = acc.i;
	c0 = do_parse_source_var_id(acc);
	if (c0.choice != -1) {
		c1 = do_parse_source_ws(acc);
		if (c1.choice != -1) {
			c2 = SyntaxStar(acc, do_parse_source_meta1);
			SyntaxTree(st_meta0, 0, i, acc.i, [[c0], [c1], c2]);
		} else c1
	} else c0
}

do_parse_source_meta1(acc : SyntaxParseAcc) -> SyntaxTree {
	i = acc.i;
	if (getCharCodeAt(acc.input, acc.i) == 44 /* , */) {
		acc.i ::= acc.i + 1;
		c1 = do_parse_source_ws(acc);
		if (c1.choice != -1) {
			c2 = do_parse_source_var_id(acc);
			if (c2.choice != -1) {
				SyntaxTree(st_meta1, 0, i, acc.i, [[c1], [c2]]);
			} else c2
		} else c1
	} else {
		// "Expected ','"
		updateExpectError(acc, "", ",", i);
		failSyntaxTree;
	}
}

do_parse_source_vars(acc : SyntaxParseAcc) -> SyntaxTree {
	i = acc.i;
	if (getCharCodeAt(acc.input, acc.i) == 40 /* ( */) {
		acc.i ::= acc.i + 1;
		c1 = do_parse_source_ws(acc);
		if (c1.choice != -1) {
			i2 = acc.i;
			t2 = do_parse_source_vars0(acc);
			c2 = if (t2.choice == -1) {acc.i ::= i2; [] } else [t2];
			if (getCharCodeAt(acc.input, acc.i) == 41 /* ) */) {
				acc.i ::= acc.i + 1;
				c4 = do_parse_source_ws(acc);
				if (c4.choice != -1) {
					SyntaxTree(st_vars, 0, i, acc.i, [[c1], c2, [c4]]);
				} else c4
			} else {
				// "Expected ')'"
				updateExpectError(acc, "", ")", i);
				failSyntaxTree;
			}
		} else c1
	} else {
		// "Expected '('"
		updateExpectError(acc, "", "(", i);
		failSyntaxTree;
	}
}

do_parse_source_vars0(acc : SyntaxParseAcc) -> SyntaxTree {
	i = acc.i;
	c0 = do_parse_source_var(acc);
	if (c0.choice != -1) {
		c1 = SyntaxStar(acc, do_parse_source_vars1);
		SyntaxTree(st_vars0, 0, i, acc.i, [[c0], c1]);
	} else c0
}

do_parse_source_vars1(acc : SyntaxParseAcc) -> SyntaxTree {
	i = acc.i;
	if (getCharCodeAt(acc.input, acc.i) == 44 /* , */) {
		acc.i ::= acc.i + 1;
		c1 = do_parse_source_ws(acc);
		if (c1.choice != -1) {
			c2 = do_parse_source_var(acc);
			if (c2.choice != -1) {
				SyntaxTree(st_vars1, 0, i, acc.i, [[c1], [c2]]);
			} else c2
		} else c1
	} else {
		// "Expected ','"
		updateExpectError(acc, "", ",", i);
		failSyntaxTree;
	}
}

do_parse_source_var(acc : SyntaxParseAcc) -> SyntaxTree {
	i = acc.i;
	c0 = do_parse_source_var_id(acc);
	if (c0.choice != -1) {
		c1 = do_parse_source_ws(acc);
		if (c1.choice != -1) {
			c2 = do_parse_source_type_id(acc);
			if (c2.choice != -1) {
				SyntaxTree(st_var, 0, i, acc.i, [[c0], [c1], [c2]]);
			} else c2
		} else c1
	} else c0
}

do_parse_source_type_id(acc : SyntaxParseAcc) -> SyntaxTree {
	i = acc.i;
	if (getCharCodeAt(acc.input, acc.i) == 58 /* : */) {
		acc.i ::= acc.i + 1;
		c1 = do_parse_source_ws(acc);
		if (c1.choice != -1) {
			c2 = do_parse_source_id(acc);
			if (c2.choice != -1) {
				c3 = do_parse_source_ws(acc);
				if (c3.choice != -1) {
					SyntaxTree(st_type_id, 0, i, acc.i, [[c1], [c2], [c3]]);
				} else c3
			} else c2
		} else c1
	} else {
		// "Expected ':'"
		updateExpectError(acc, "", ":", i);
		failSyntaxTree;
	}
}

do_parse_source_expr(acc : SyntaxParseAcc) -> SyntaxTree {
	i = acc.i;
	c0 = do_parse_source_type_id(acc);
	if (c0.choice != -1) {
		if (getCharCodeAt(acc.input, acc.i) == 61 /* = */) {
			acc.i ::= acc.i + 1;
			c2 = do_parse_source_ws(acc);
			if (c2.choice != -1) {
				c3 = do_parse_source_expr__choice57(acc);
				if (c3.choice != -1) {
					c4 = do_parse_source_ws(acc);
					if (c4.choice != -1) {
						c5 = SyntaxPlus(acc, do_parse_source_symbws);
						if (length(c5) >= 1) {
							if (getCharCodeAt(acc.input, acc.i) == 59 && getCharCodeAt(acc.input, acc.i + 1) == 59 /* ;; */) {
								acc.i ::= acc.i + 2;
								c7 = do_parse_source_ws(acc);
								if (c7.choice != -1) {
									SyntaxTree(st_expr, 0, i, acc.i, [[c0], [c2], [c3], [c4], c5, [c7]]);
								} else c7
							} else {
								// "Expected ';;'"
								updateExpectError(acc, "", ";;", i);
								failSyntaxTree;
							}
						} else {
							// "Expected at least one symbws"
							updateExpectError(acc, "", "symbws", i);
							failSyntaxTree;
						}
					} else c4
				} else c3
			} else c2
		} else {
			// "Expected '='"
			updateExpectError(acc, "", "=", i);
			failSyntaxTree;
		}
	} else c0
}

do_parse_source_symbws(acc : SyntaxParseAcc) -> SyntaxTree {
	i = acc.i;
	c0 = do_parse_source_symb(acc);
	if (c0.choice != -1) {
		c1 = do_parse_source_ws(acc);
		if (c1.choice != -1) {
			SyntaxTree(st_symbws, 0, i, acc.i, [[c0], [c1]]);
		} else c1
	} else c0
}

do_parse_source_symb(acc : SyntaxParseAcc) -> SyntaxTree {
	i = acc.i;
	c0 = SyntaxPlus(acc, do_parse_source_symb__auto60);
	if (length(c0) >= 1) {
		SyntaxTree(st_symb, 0, i, acc.i, [c0]);
	} else {
		// "Expected at least one symb$auto60"
		updateExpectError(acc, "", "symb$auto60", i);
		failSyntaxTree;
	}
}

do_parse_source_ind(acc : SyntaxParseAcc) -> SyntaxTree {
	i = acc.i;
	c0 = SyntaxPlus(acc, do_parse_source_digit);
	if (length(c0) >= 1) {
		SyntaxTree(st_ind, 0, i, acc.i, [c0]);
	} else {
		// "Expected at least one digit"
		updateExpectError(acc, "", "digit", i);
		failSyntaxTree;
	}
}

do_parse_source_id(acc : SyntaxParseAcc) -> SyntaxTree {
	i = acc.i;
	c0 = SyntaxPlus(acc, do_parse_source_id_char);
	if (length(c0) >= 1) {
		SyntaxTree(st_id, 0, i, acc.i, [c0]);
	} else {
		// "Expected at least one id_char"
		updateExpectError(acc, "", "id_char", i);
		failSyntaxTree;
	}
}

do_parse_source_var_id(acc : SyntaxParseAcc) -> SyntaxTree {
	i = acc.i;
	c0 = SyntaxPlus(acc, do_parse_source_var_id_char);
	if (length(c0) >= 1) {
		SyntaxTree(st_var_id, 0, i, acc.i, [c0]);
	} else {
		// "Expected at least one var_id_char"
		updateExpectError(acc, "", "var_id_char", i);
		failSyntaxTree;
	}
}

do_parse_source_id_char(acc : SyntaxParseAcc) -> SyntaxTree {
	i = acc.i;
	acc.i ::= i;
	r0 = {
		if ({c = getCharCodeAt(acc.input, acc.i); 97 <= c && c <= 122}) {
			acc.i ::= acc.i + 1;
			SyntaxTree(st_id_char, 0, i, acc.i, []);
		} else failSyntaxTree;
	}
	if (r0 != failSyntaxTree) r0
	else {
		acc.i ::= i;
		r1 = {
			if ({c = getCharCodeAt(acc.input, acc.i); 65 <= c && c <= 90}) {
				acc.i ::= acc.i + 1;
				SyntaxTree(st_id_char, 1, i, acc.i, []);
			} else failSyntaxTree;
		}
		if (r1 != failSyntaxTree) r1
		else {
			acc.i ::= i;
			r2 = {
				if ({c = getCharCodeAt(acc.input, acc.i); 48 <= c && c <= 57}) {
					acc.i ::= acc.i + 1;
					SyntaxTree(st_id_char, 2, i, acc.i, []);
				} else failSyntaxTree;
			}
			if (r2 != failSyntaxTree) r2
			else {
				acc.i ::= i;
				r3 = {
					if (getCharCodeAt(acc.input, acc.i) == 95 /* _ */) {
						acc.i ::= acc.i + 1;
						SyntaxTree(st_id_char, 3, i, acc.i, []);
					} else failSyntaxTree;
				}
				if (r3 != failSyntaxTree) r3
				else {
					acc.i ::= i;
					r4 = {
						if (getCharCodeAt(acc.input, acc.i) == 46 /* . */) {
							acc.i ::= acc.i + 1;
							SyntaxTree(st_id_char, 4, i, acc.i, []);
						} else failSyntaxTree;
					}
					if (r4 != failSyntaxTree) r4
					else {
						acc.i ::= i;
						r5 = {
							if (getCharCodeAt(acc.input, acc.i) == 45 /* - */) {
								acc.i ::= acc.i + 1;
								SyntaxTree(st_id_char, 5, i, acc.i, []);
							} else failSyntaxTree;
						}
						if (r5 != failSyntaxTree) r5
						else {
							acc.i ::= i;
							r6 = {
								if (getCharCodeAt(acc.input, acc.i) == 39 /* ' */) {
									acc.i ::= acc.i + 1;
									SyntaxTree(st_id_char, 6, i, acc.i, []);
								} else failSyntaxTree;
							}
							if (r6 != failSyntaxTree) r6
							else {
								SyntaxTree(st_Expected_id_char, -1, i, i, [])
							}

						}

					}

				}

			}

		}

	}

}

do_parse_source_letter(acc : SyntaxParseAcc) -> SyntaxTree {
	i = acc.i;
	acc.i ::= i;
	r0 = {
		if ({c = getCharCodeAt(acc.input, acc.i); 97 <= c && c <= 122}) {
			acc.i ::= acc.i + 1;
			SyntaxTree(st_letter, 0, i, acc.i, []);
		} else failSyntaxTree;
	}
	if (r0 != failSyntaxTree) r0
	else {
		acc.i ::= i;
		r1 = {
			if ({c = getCharCodeAt(acc.input, acc.i); 65 <= c && c <= 90}) {
				acc.i ::= acc.i + 1;
				SyntaxTree(st_letter, 1, i, acc.i, []);
			} else failSyntaxTree;
		}
		if (r1 != failSyntaxTree) r1
		else {
			SyntaxTree(st_Expected_letter, -1, i, i, [])
		}

	}

}

do_parse_source_digit(acc : SyntaxParseAcc) -> SyntaxTree {
	i = acc.i;
	if ({c = getCharCodeAt(acc.input, acc.i); 48 <= c && c <= 57}) {
		acc.i ::= acc.i + 1;
		SyntaxTree(st_digit, 0, i, acc.i, []);
	} else {
		// "Expected '0'-'9'"
		updateExpectError(acc, "", "'0'-'9'", i);
		failSyntaxTree;
	}
}

do_parse_source_non_space_char(acc : SyntaxParseAcc) -> SyntaxTree {
	i = acc.i;
	if ({c = getCharCodeAt(acc.input, acc.i); 33 <= c && c <= 65535}) {
		acc.i ::= acc.i + 1;
		SyntaxTree(st_non_space_char, 0, i, acc.i, []);
	} else {
		// "Expected '!'-'￿'"
		updateExpectError(acc, "", "'!'-'￿'", i);
		failSyntaxTree;
	}
}

do_parse_source_var_id_char(acc : SyntaxParseAcc) -> SyntaxTree {
	i = acc.i;
	acc.i ::= i;
	r0 = {
		if ({c = getCharCodeAt(acc.input, acc.i); 33 <= c && c <= 40}) {
			acc.i ::= acc.i + 1;
			SyntaxTree(st_var_id_char, 0, i, acc.i, []);
		} else failSyntaxTree;
	}
	if (r0 != failSyntaxTree) r0
	else {
		acc.i ::= i;
		r1 = {
			if (getCharCodeAt(acc.input, acc.i) == 42 /* * */) {
				acc.i ::= acc.i + 1;
				SyntaxTree(st_var_id_char, 1, i, acc.i, []);
			} else failSyntaxTree;
		}
		if (r1 != failSyntaxTree) r1
		else {
			acc.i ::= i;
			r2 = {
				if (getCharCodeAt(acc.input, acc.i) == 43 /* + */) {
					acc.i ::= acc.i + 1;
					SyntaxTree(st_var_id_char, 2, i, acc.i, []);
				} else failSyntaxTree;
			}
			if (r2 != failSyntaxTree) r2
			else {
				acc.i ::= i;
				r3 = {
					if ({c = getCharCodeAt(acc.input, acc.i); 45 <= c && c <= 57}) {
						acc.i ::= acc.i + 1;
						SyntaxTree(st_var_id_char, 3, i, acc.i, []);
					} else failSyntaxTree;
				}
				if (r3 != failSyntaxTree) r3
				else {
					acc.i ::= i;
					r4 = {
						if ({c = getCharCodeAt(acc.input, acc.i); 59 <= c && c <= 65535}) {
							acc.i ::= acc.i + 1;
							SyntaxTree(st_var_id_char, 4, i, acc.i, []);
						} else failSyntaxTree;
					}
					if (r4 != failSyntaxTree) r4
					else {
						SyntaxTree(st_Expected_var_id_char, -1, i, i, [])
					}

				}

			}

		}

	}

}

do_parse_source_char_(acc : SyntaxParseAcc) -> SyntaxTree {
	i = acc.i;
	if ({c = getCharCodeAt(acc.input, acc.i); 0 <= c && c <= 65535}) {
		acc.i ::= acc.i + 1;
		SyntaxTree(st_char_, 0, i, acc.i, []);
	} else {
		// "Expected ' '-'￿'"
		updateExpectError(acc, "", "' '-'￿'", i);
		failSyntaxTree;
	}
}

do_parse_source_ws(acc : SyntaxParseAcc) -> SyntaxTree {
	i = acc.i;
	c0 = SyntaxStar(acc, do_parse_source_space);
	SyntaxTree(st_ws, 0, i, acc.i, [c0]);
}

do_parse_source_space(acc : SyntaxParseAcc) -> SyntaxTree {
	i = acc.i;
	acc.i ::= i;
	r0 = {
		if (getCharCodeAt(acc.input, acc.i) == 32 /*   */) {
			acc.i ::= acc.i + 1;
			SyntaxTree(st_space, 0, i, acc.i, []);
		} else failSyntaxTree;
	}
	if (r0 != failSyntaxTree) r0
	else {
		acc.i ::= i;
		r1 = {
			if (getCharCodeAt(acc.input, acc.i) == 9 /* 	 */) {
				acc.i ::= acc.i + 1;
				SyntaxTree(st_space, 1, i, acc.i, []);
			} else failSyntaxTree;
		}
		if (r1 != failSyntaxTree) r1
		else {
			acc.i ::= i;
			r2 = {
				if (getCharCodeAt(acc.input, acc.i) == 10 /* 
				 */) {
					acc.i ::= acc.i + 1;
					SyntaxTree(st_space, 2, i, acc.i, []);
				} else failSyntaxTree;
			}
			if (r2 != failSyntaxTree) r2
			else {
				acc.i ::= i;
				r3 = {
					if (getCharCodeAt(acc.input, acc.i) == 13 /* 
 */) {
						acc.i ::= acc.i + 1;
						SyntaxTree(st_space, 3, i, acc.i, []);
					} else failSyntaxTree;
				}
				if (r3 != failSyntaxTree) r3
				else {
					SyntaxTree(st_Expected_space, -1, i, i, [])
				}

			}

		}

	}

}

do_parse_source_expr__choice57(acc : SyntaxParseAcc) -> SyntaxTree {
	i = acc.i;
	acc.i ::= i;
	r0 = {
		if (getCharCodeAt(acc.input, acc.i) == 35 /* # */) {
			acc.i ::= acc.i + 1;
			SyntaxTree(st_expr__choice57, 0, i, acc.i, []);
		} else failSyntaxTree;
	}
	if (r0 != failSyntaxTree) r0
	else {
		acc.i ::= i;
		r1 = {
			if (getCharCodeAt(acc.input, acc.i) == 124 && getCharCodeAt(acc.input, acc.i + 1) == 45 /* |- */) {
				acc.i ::= acc.i + 2;
				SyntaxTree(st_expr__choice57, 1, i, acc.i, []);
			} else failSyntaxTree;
		}
		if (r1 != failSyntaxTree) r1
		else {
			SyntaxTree(st_Expected_expr__choice57, -1, i, i, [])
		}

	}

}

do_parse_source_scomment__auto58(acc : SyntaxParseAcc) -> SyntaxTree {
	i = acc.i;
	if (getCharCodeAt(acc.input, acc.i) == 10 /* 
	 */) {
		SyntaxTree(-10, -1, i, i, [])
	} else {
		c1 = do_parse_source_char_(acc);
		if (c1.choice != -1) {
			SyntaxTree(st_scomment__auto58, 0, i, acc.i, []);
		} else c1
	}
}

do_parse_source_mcomment__auto59(acc : SyntaxParseAcc) -> SyntaxTree {
	i = acc.i;
	if (getCharCodeAt(acc.input, acc.i) == 42 && getCharCodeAt(acc.input, acc.i + 1) == 47/* * / */) {
		SyntaxTree(-42, -1, i, i, [])
	} else {
		c1 = do_parse_source_char_(acc);
		if (c1.choice != -1) {
			SyntaxTree(st_mcomment__auto59, 0, i, acc.i, []);
		} else c1
	}
}

do_parse_source_symb__auto60(acc : SyntaxParseAcc) -> SyntaxTree {
	i = acc.i;
	if (getCharCodeAt(acc.input, acc.i) == 59 && getCharCodeAt(acc.input, acc.i + 1) == 59 /* ;; */) {
		SyntaxTree(-59, -1, i, i, [])
	} else {
		c1 = do_parse_source_non_space_char(acc);
		if (c1.choice != -1) {
			SyntaxTree(st_symb__auto60, 0, i, acc.i, [[c1]]);
		} else c1
	}
}

