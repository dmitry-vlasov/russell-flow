import ru/src;

export {
	RuDecls(
		consts     : Tree<int, RuConst>,
		types      : Tree<int, RuType>,
		rules      : Tree<int, RuRule>,
		assertions : Tree<int, RuAssertion>
	);

	ruEmptyDecls = RuDecls(makeTree(), makeTree(), makeTree(), makeTree());
}
