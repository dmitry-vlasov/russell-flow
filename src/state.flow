import ru/math_utils;
import mm/src_utils;

export {
	RuState(
		ru   : Tree<int, RuSource>, 
		mm   : Tree<int, MmSource>,
		vars : Tree<string, ValValue>,
		conf : RuConf
	);

	ruEmptyState = RuState(makeTree(), makeTree(), makeTree(), ruEmptyConf);
}
