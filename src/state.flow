import ru/math_utils;
import mm/src_utils;

export {
	RuState(
		ru   : Tree<int, RuSource>, 
		mm   : Tree<int, MmSource>,
		vars : Tree<string, ValValue>,
		conf : RuConf,
		depth : int
	);

	ruEmptyState = RuState(makeTree(), makeTree(), makeTree(), ruEmptyConf, 0);
}
