import ru/math_utils;
import mm/src_utils;
import ru/prover/env;

export {
	RuState(
		ru   : Tree<int, RuSource>, 
		mm   : Tree<int, MmSource>,
		prove: Maybe<RuProverEnv>,
		vars : Tree<string, ValValue>,
		conf : RuConf
	);

	ruEmptyState = RuState(makeTree(), makeTree(), None(), makeTree(), ruEmptyConf);
}
