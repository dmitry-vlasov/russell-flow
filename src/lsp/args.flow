import base/out;
import script/script;

export {
	RuLspArgs(
		conf : RuConf,
		id : Json, 
		params : Json, 
		out_cb : (Json) ->  void, 
		exit_cb : () -> void,
		process : (ScScript, (int) -> void, RuOut) -> void
	);
}
