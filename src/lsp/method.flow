import script/script;
import base/env;
import lsp/state;

export {
	RuLspMethod(
		request2script : (Json, RuEnv) -> ScComm,
		changeState : (Json, RuLsp) -> RuLsp,
		responses : bool,
		callback : (string, RuEnv) -> void,
		async : bool
	);

	ruLspKeepState(json : Json, state : RuLsp) -> RuLsp {
		state;
	}
	ruLspEmptyComm(json : Json, env : RuEnv) -> ScComm {
		 ScAction("stop", []);
	}
}
