import script/func;
import script/util;

export {
	ruScriptBooleanFuncs() -> [ScriptFn];
}

ruScriptBooleanFuncs() -> [ScriptFn] {
	[
		ScriptFn("&&",  \args, state, out -> ruEvalBoolFn(args,  \as -> Some(scBoolVal(all(as))), "&&", -1)),
		ScriptFn("||",  \args, state, out -> ruEvalBoolFn(args,  \as -> Some(scBoolVal(any(as))), "||", -1)),
		ScriptFn("!",   \args, state, out  -> ruEvalBoolFn(args, \as -> Some(scBoolVal(!as[0])), "!", 1))
	]
}
