import script/func;

export {
	ruScriptBooleanFuncs() -> [Pair<string, ScriptFn>];
}

ruScriptBooleanFuncs() -> [Pair<string, ScriptFn>] {
	[
		Pair("&&", ScriptFn(\args, state, out -> 
			switch (args[0]) {
				ValBool(__): ruEvalBinaryBoolFn(args, \a, b -> a && b);
				default: None();
			}
		)),
		Pair("||", ScriptFn(\args, state, out -> 
			switch (args[0]) {
				ValBool(__): ruEvalBinaryBoolFn(args, \a, b -> a || b);
				default: None();
			}
		)),
		Pair("!", ScriptFn(\args, state, out -> 
			switch (args[0]) {
				ValBool(v): Some(ValBool(!v));
				default: None();
			}
		)),
	]
}
