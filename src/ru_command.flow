import ru_math;
import mm_src;

export {
	RuEnv(
		ruMath : RuMath, 
		mmMath : MmMath
	);
	RuCommArg(
		name : string,
		optional : bool,
		help : string,
		defval : string
	);
	RuComm(
		name : string,
		category : string,
		help : string,
		args : [RuCommArg],
		run : (RuConf, RuTask, RuEnv) -> RuEnv
	);
}
